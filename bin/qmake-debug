#!/bin/bash

if [ -z ${QMAKE:-} ]
then
    QMAKE=$(which qmake)
fi

SPEC=${SPEC:-linux-clang-libc++}

readonly LFLAGS="-L/usr/lib -lprofiler"

set -e -x

"$QMAKE" \
    -spec "$SPEC" \
    CONFIG+=debug \
    DEFINES+=USE_GOOGLE_PROFILER \
    QMAKE_LFLAGS*="$LFLAGS" \
    "$@"
