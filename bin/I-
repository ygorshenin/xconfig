#!/bin/bash

if [ -z "$winid" ]; then
  sed 's/^  //'
  exit 0
fi

tmp=/tmp/I-.tmp.$$
trap "rm -f $tmp; exit 1" 1 2 15

9p read acme/$winid/rdsel >$tmp
sed 's/^  //' $tmp | 9p write acme/$winid/wrsel
rm -f $tmp
